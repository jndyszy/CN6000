# CN6000 完整部署指南
**环境：AWS EC2 (Amazon Linux) | 公网 IP: 18.141.229.33**

---

## 一、本地文件结构

部署前，你的项目目录需要整理成如下结构：

```
cn6000/
├── docker-compose.yml     ← 唯一的 compose 文件（已提供）
├── .env                   ← 后端环境变量（见下方说明）
├── init.sql               ← 数据库初始化脚本（已有）
├── frontend/
│   ├── Dockerfile         ← 前端 Dockerfile（已有）
│   ├── nginx.conf         ← 已更新，加入了 API 反代（已提供）
│   └── src/ ...           ← 前端源码
└── backend/
    ├── Dockerfile         ← 后端 Dockerfile（已有）
    ├── go.mod
    ├── go.sum
    └── ...                ← 后端源码
```

---

## 二、.env 文件

在项目根目录创建 `.env`，填入后端需要的环境变量，例如：

```env
JWT_SECRET=你的jwt密钥
# 其他后端需要的变量...
```

> DATABASE_URL 和 REDIS_ADDR 已经在 docker-compose.yml 里直接写好，不需要放进 .env。

---

## 三、AWS Security Group 设置

在 AWS 控制台 → EC2 → Security Groups，确保入站规则（Inbound rules）包含：

| 类型  | 协议 | 端口 | 来源      |
|-------|------|------|-----------|
| SSH   | TCP  | 22   | 你的 IP   |
| HTTP  | TCP  | 80   | 0.0.0.0/0 |

> 5432（PostgreSQL）和 6379（Redis）**不要对外开放**，compose 里已绑定 127.0.0.1。

---

## 四、EC2 安装 Docker

SSH 进入服务器：

```bash
ssh -i 你的密钥.pem ec2-user@18.141.229.33
```

**Amazon Linux 2023：**
```bash
sudo dnf update -y
sudo dnf install -y docker
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER
newgrp docker
```

**Amazon Linux 2：**
```bash
sudo yum update -y
sudo amazon-linux-extras install docker -y
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER
newgrp docker
```

验证安装成功：
```bash
docker --version
```

---

## 五、安装 Docker Compose

```bash
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" \
  -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
docker-compose --version
```

---

## 六、上传项目文件到服务器

在**本地**执行，把整个项目目录传到服务器：

```bash
scp -i 你的密钥.pem -r ./cn6000 ec2-user@18.141.229.33:~/cn6000
```

---

## 七、启动服务

SSH 进服务器，进入项目目录：

```bash
cd ~/cn6000
docker compose up -d --build
```

首次启动会拉取镜像、编译前后端，时间稍长（3~5 分钟），耐心等待。

---

## 八、验证部署

```bash
# 查看所有服务状态（应全部为 Up）
docker compose ps

# 查看数据库是否建表成功
docker exec postgresql psql -U admin -d postgres -c "\dt"

# 查看各服务实时日志
docker compose logs -f
```

浏览器访问：**http://18.141.229.33**

---

## 九、常用运维命令

```bash
# 重启所有服务
docker compose restart

# 只重启某个服务（如后端）
docker compose restart app

# 查看某个服务的日志
docker compose logs -f app
docker compose logs -f postgres

# 停止所有服务
docker compose down

# 更新代码后重新构建并重启
docker compose up -d --build

# 查看容器资源占用（t3.micro 内存只有 1GB，注意监控）
docker stats
```

---

## 十、重新初始化数据库（慎用）

如果需要清空数据库重新执行 init.sql：

```bash
docker compose down
docker volume rm cn6000_pgdata
docker compose up -d --build
```

> ⚠️ 这会删除所有数据，生产环境慎用。
