# BlueNote（小蓝书）本地部署指南
**环境：本地开发机（Windows / macOS / Linux）+ Docker**

---

## 一、前置条件

- 安装 [Docker Desktop](https://www.docker.com/products/docker-desktop/)（Windows / macOS）或 Docker Engine + Docker Compose 插件（Linux）
- 一个开启了[应用专用密码](https://support.google.com/accounts/answer/185833)的 Gmail 账号（用于发送注册 / 重置密码验证码邮件）

验证安装成功：
```bash
docker --version
docker compose version
```

---

## 二、本地文件结构

项目目录需要整理成如下结构（与仓库实际目录一致，前后端源码各嵌套在一层子目录中）：

```
bluenote/
├── docker-compose.yml          ← 唯一的 compose 文件（已提供）
├── .env                        ← 后端环境变量（见下方说明）
├── init.sql                    ← 数据库初始化脚本（已有）
├── frontend/
│   ├── Dockerfile              ← 前端 Dockerfile（已有）
│   ├── nginx.conf              ← 已更新，加入了 API 反代（已提供）
│   └── my-app/
│       ├── package.json
│       └── src/ ...            ← 前端源码
└── backend/
    ├── Dockerfile               ← 后端 Dockerfile（已有）
    └── my-backend/
        ├── go.mod
        ├── go.sum
        └── ...                  ← 后端源码
```

```bash
git clone https://github.com/jndyszy/bluenote.git bluenote
cd bluenote
```

---

## 三、.env 文件

在项目根目录创建 `.env`，填入后端需要的环境变量：

```env
JWT_SECRET=你的jwt密钥
GMAIL_FROM=你的Gmail地址
GMAIL_PASSWORD=你的Gmail应用专用密码
BASE_URL=http://localhost
```

| 变量               | 说明                                              |
|--------------------|---------------------------------------------------|
| `JWT_SECRET`       | JWT (HS256) 签名密钥，随便填一串随机字符串即可        |
| `GMAIL_FROM`       | 用于发送验证码邮件的 Gmail 地址                     |
| `GMAIL_PASSWORD`   | Gmail 应用专用密码（不是登录密码，需在 Google 账号中单独生成） |
| `BASE_URL`         | 上传图片的访问前缀，本地部署固定填 `http://localhost` |

> DATABASE_URL 和 REDIS_ADDR 已经在 docker-compose.yml 里直接写好，不需要放进 .env。

---

## 四、启动服务

在项目根目录执行：

```bash
docker compose up -d --build
```

首次启动会拉取镜像、编译前后端，时间稍长（3~5 分钟），耐心等待。

这会启动 4 个容器：

| 容器       | 说明                                | 端口                  |
|------------|-------------------------------------|-----------------------|
| `frontend` | Nginx 提供 React SPA                | `80`（本机可访问）      |
| `app`      | Go 后端                             | `127.0.0.1:8080`（仅本机） |
| `postgres` | PostgreSQL 15，数据持久化到 `pgdata` | 仅容器内网              |
| `redis`    | Redis 7，数据持久化到 `redisdata`    | 仅容器内网              |

---

## 五、验证部署

```bash
# 查看所有服务状态（应全部为 Up）
docker compose ps

# 查看数据库是否建表成功
docker exec postgresql psql -U admin -d postgres -c "\dt"

# 查看各服务实时日志
docker compose logs -f
```

浏览器访问：**http://localhost**

---

## 六、常用运维命令

```bash
# 重启所有服务
docker compose restart

# 只重启某个服务（如后端）
docker compose restart app

# 查看某个服务的日志
docker compose logs -f app
docker compose logs -f postgres

# 停止所有服务
docker compose down

# 更新代码后重新构建并重启
docker compose up -d --build

# 查看容器资源占用
docker stats
```

---

## 七、重新初始化数据库（慎用）

如果需要清空数据库重新执行 init.sql：

```bash
docker compose down
docker volume rm bluenote_pgdata
docker compose up -d --build
```

> ⚠️ 这会删除所有数据，请确认无误后再执行。

> 提示：卷名前缀由项目目录名决定（Docker Compose 默认规则），如果你的项目文件夹名不是 `bluenote`，卷名要相应替换，可用 `docker volume ls` 查看实际名称。
